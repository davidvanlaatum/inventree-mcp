# Tool Reference

This file is the operator-facing and agent-facing reference for registered MCP tools. Keep it aligned with the checked tool authorization manifest in `docs/tool-manifest.json`, `docs/endpoint-manifest.yaml`, and the registered Go structs.

`inventree-mcp self-update` is deliberately absent from this reference and the generated tool manifest. It is a local CLI command only and is never callable through an MCP STDIO or HTTP session; see [Local CLI self-update policy](self-update.md).

## Checked Tool Manifest

`docs/tool-manifest.json` is generated from `internal/tools.ToolAuthorizations`, `internal/tools.PromptManifest`, and `internal/tools.InvenTreeIcon` with:

```sh
go generate ./internal/tools
```

Every registered tool and the MCP server implementation identity publish the official InvenTree documentation logo at `https://docs.inventree.org/en/latest/assets/logo.png` with MIME type `image/png`. The checked manifest records the per-tool icon metadata. Rendering remains client-controlled, so an MCP client may ignore the icon, cache an older image, or continue to show its generic tool glyph. Because the `en/latest` asset is externally hosted and intentionally follows current upstream branding, its availability, content, or MIME behavior can change independently of an `inventree-mcp` release. A client that renders the icon may make a cross-origin request to `docs.inventree.org`, exposing network metadata such as its egress IP, user agent, and request timing. Clients should fetch it without credentials, validate the decoded image independently of the advisory MIME type, and may reject it under stricter external-content policy.

The manifest is checked in and tested. It records each registered tool's `milestone_1` status, mutation class (`read_only`, `write`, `operational`, or `destructive`), required scopes, MCP annotation booleans, upload source class, and HTTP registration state. Read-only tools and `health_version` are `registered` for HTTP development mode. Production HTTP startup registers write, operational, upload, image, and destructive tools only with OAuth authorization mode enabled, where they are `registered_with_oauth_scope_guard`.

## Manifest Fields

Each registered tool must have:

- Tool name.
- Workflow group.
- Milestone status: `milestone_1`, `future`, or `deferred`.
- Mutation class: `read_only`, `write`, `operational`, or `destructive`.
- MCP annotations: read-only, destructive, idempotent, and open-world behavior.
- Standard MCP icon source and MIME type.
- Required OAuth scopes.
- Accepted upload sources, when relevant: `inline_base64`, `stdio_local_path`, `http_url_fetch`, `http_url_link`, or `existing_attachment_image`.
- Stable retry fields for clarification responses.
- "Ask operator when..." guidance.

Endpoint-backed tools must also map to a `docs/endpoint-manifest.yaml` entry whose path, method, operation ID, selected query filters, request schema, and response schema are validated against `docs/api-schema.yaml`.

In OAuth authorization mode, scoped tools publish per-tool OAuth metadata in descriptor `_meta["securitySchemes"]` and `_meta["openai/securitySchemes"]`, matching the checked tool authorization manifest. The current Go MCP SDK `mcp.Tool` type does not expose a first-class top-level `securitySchemes` field; replace the mirror-only descriptor wiring with the canonical field when the SDK adds support or when the server owns custom tool descriptor serialization.

Production connector linking uses the configured authorization-server metadata, CIMD client metadata, `private_key_jwt` assertions verified against a same-origin JWKS, and PKCE S256. The setup page validates a submitted InvenTree Token or Bearer credential and prefers a uniquely named dedicated connector token that does not rotate existing keys; sealing the supplied credential requires an explicit fallback choice. Abandoned or expired authorizations may require operator revocation of unused `inventree-mcp-chatgpt-*` tokens. MCP access/refresh/code/setup values are encrypted envelopes or one-time bounded state, and upstream credentials are never part of tool results or the MCP traffic log.

## MCP Protocol Behavior

The server uses official Go MCP SDK `v1.7.0`. New clients negotiate MCP `2026-07-28` through `server/discover`; legacy clients can still use `initialize`. Streamable HTTP is stateless and sessionless: it does not issue or honor `Mcp-Session-Id`, and standalone GET/DELETE session operations return `405 Method Not Allowed`. Aborting a `2026-07-28` POST cancels the in-flight tool handler.

Both discovery paths publish advisory server instructions for agents that encounter a missing `inventree-mcp` tool or capability. The instructions require the agent to distinguish a server gap from bad or ambiguous input, insufficient OAuth scopes or InvenTree permissions, server/configuration failures, and upstream InvenTree limitations; explain the gap and any safe existing workaround; and avoid silently substituting a materially different operation. With GitHub search access, the agent should search both open and closed [project issues](https://github.com/davidvanlaatum/inventree-mcp/issues), report existing coverage without creating a duplicate, and ask the operator before creating an untracked issue. Without GitHub search access, it should disclose that limitation and ask whether the operator wants the gap checked and, if untracked, an issue created. MCP clients decide how to apply server instructions, so this guidance cannot guarantee agent behavior.

HTTP request bodies are bounded by `INVENTREE_MCP_MAX_REQUEST_BODY_BYTES` or `--mcp-max-request-body-bytes`. The default is 8 MiB. Configuration validation requires this limit to cover the configured inline upload maximum after base64 expansion plus 1 MiB of JSON and tool-argument overhead.

SDK `v1.7.0` serializes `readOnlyHint` and `idempotentHint` even when false. The registered annotation booleans in this document therefore match the JSON wire representation as well as the checked manifest.

## Lookup Tool Framework

Read-only lookup handlers use a context-resolved InvenTree client supplied through the tool dependency struct. Handlers depend on the lookup client interface instead of constructing a concrete HTTP client, so STDIO credentials and future HTTP OAuth credentials stay in the server layer.

Common lookup inputs:

| Field | Applies to | Behavior |
| --- | --- | --- |
| `search` | search tools | Optional text passed to the schema-backed InvenTree endpoint. |
| `limit` | list/search tools | Optional maximum result count. Defaults to `20` and is capped at `100`. |
| `offset` | list/search tools | Optional pagination offset for deterministic retries. |
| `id` | get-by-id tools | Stable InvenTree primary key. |
| `model_type` | `list_attachments` and attachment creation/upload tools | Attachment endpoint model type. Use only the short, unqualified in-scope values `part`, `stockitem`, `company`, `manufacturerpart`, `supplierpart`, or `purchaseorder`; qualified parameter values such as `part.part` and `order.purchaseorder` are invalid for attachment tools. Attachment metadata update and delete tools identify the target by attachment `id` instead. |
| `model_type` | parameter-template administration | Optional parameter endpoint restriction. Use an explicit empty string for no restriction or one of the qualified `app.model` values `build.build`, `company.company`, `company.manufacturerpart`, `company.supplierpart`, `order.purchaseorder`, `order.returnorder`, `order.salesorder`, `order.salesordershipment`, `order.transferorder`, `part.part`, `part.partcategory`, or `stock.stocklocation`; short attachment values such as `part` and `purchaseorder` are invalid. |
| `model_id` | attachment tools and parameter-row outputs | Stable primary key interpreted using the endpoint-specific `model_type`. Part-parameter tools accept `part_id` and supply `part.part` internally rather than asking callers for a model type. |
| `part_id` | part parameter and stock-item tools | Stable part primary key for part-scoped reads or stock duplicate checks. |
| `parameter_id`, `part_name`, `category_id`, `template_id`, `template_name`, `units`, `value`, `verified` | cross-inventory parameter tools | Stable row/part/category/template identity, review metadata, exact value, and confirmed deletion read-back result. |
| `location_id` | stock-item tools | Stable stock location primary key. Required for `create_stock_item`; optional for stock duplicate-check searches. |
| `quantity` | `create_stock_item` | Initial stock quantity. Must be greater than zero. |
| `status` | `create_stock_item` | Optional InvenTree stock status code when local convention requires one. |
| `batch`, `serial`, `notes` | `create_stock_item` | Optional stock-item metadata passed through to the schema-backed stock endpoint. |
| `dry_run` | workflow tools | Return planned actions without writing when true. |
| `stock_item_id`, `reason`, `confirm`, `plan_hash` | stock adjustment and depletion tools | One stable stock-item ID, a required nonblank audit reason, explicit execution confirmation, and the opaque principal-bound five-minute single-use token returned by the latest dry run. A newer dry run for the same action/item supersedes the prior token; outstanding storage is bounded. |
| `part_ids`, `category_id`, `include_subcategories`, `overwrite_existing`, `confirm`, `plan_hash` | `bulk_propagate_part_parameters` | Exactly one bounded part selector, explicit overwrite intent, and the principal-bound five-minute single-use token from the matching dry run. A plan covers at most 100 parts; a newer matching plan supersedes the prior token. |
| `delta` | `adjust_stock_quantity` | Non-zero relative quantity change. Positive values add stock; negative values remove stock and are marked high-risk. |
| `observed_quantity` | `stocktake_adjustment` | Absolute non-negative physical count. Stocktake does not implicitly change location, status, batch, serial, or packaging. |
| `status` | `set_stock_status` | One schema-supported InvenTree stock status code. `Destroyed`, `Rejected`, and `Lost` are marked high-risk. |
| `supplier_name`, `manufacturer_name` | `upsert_part_with_supplier_and_manufacturer` | Company names used to prefer existing supplier/manufacturer records or create them when unambiguous. |
| `supplier_currency`, `manufacturer_currency` | `upsert_part_with_supplier_and_manufacturer` | Currency required before creating a supplier or manufacturer company. |
| `supplier_sku`, `mpn` | `upsert_part_with_supplier_and_manufacturer` | Supplier SKU is required for a supplier-part link. MPN is optional; null, blank, and whitespace-only input is normalized to omission and no fallback value is invented. With no MPN, the workflow reuses one exact part/manufacturer link, clarifies multiple links, or records `skip_manufacturer_part` when none exists. |
| `actions`, `record_type`, `reason` | workflow outputs | Ordered plan or execution summary for workflow-level tools. Action summaries describe what will happen but do not replace the effective field-level dry-run plan. |
| `planned_changes`, `action`, `fields`, `depends_on` | workflow dry-run outputs | Effective upstream create or PATCH fields for each planned mutation. `id` identifies an existing mutation target; each `depends_on` entry maps one unresolved foreign-key `field` to the earlier planned create `action` that will supply it. Reuse and skip actions remain in `actions` and do not produce mutation entries. These previews are advisory rather than confirmation tokens: execution repeats preflight and can return a different result after upstream state changes. |
| `part`, `supplier`, `manufacturer`, `supplier_part`, `manufacturer_part` | `upsert_part_with_supplier_and_manufacturer` output | Factual stable records selected, reused, updated, or created by the workflow. Planned creates without stable IDs are represented only in `planned_changes`, never as synthetic zero-ID records. |
| `validation`, `status_code`, `fields`, `messages` | write and workflow failure outputs | Bounded allowlisted InvenTree validation details with canonical non-echoing messages. |
| `failure`, `failure.validation`, `remaining_actions`, `recovery_plan` | `upsert_part_with_supplier_and_manufacturer` output | Failed action, nested safe validation details when available, not-yet-attempted workflow steps, and a stable-ID read/search recovery plan. Earlier selected or created records and completed actions remain present. |
| `part_search`, `location_search` | `create_initial_stock_entry` | Human-readable lookup text used only when stable `part_id` or `location_id` is omitted. Ambiguous matches return clarification. |
| `location`, `stock_item` | `create_initial_stock_entry` output | Stable location and created stock item selected or written by the workflow. Dry runs expose the effective stock-item create fields in `planned_changes`; `stock_item` remains absent until execution returns a factual record. |
| `lines`, `supplier_part_id`, `supplier_sku`, `unit_price`, `currency`, `line_total`, `warnings`, `index` | `preview_purchase_order_with_lines` | No-write purchase preview line inputs and outputs. Supplier-part IDs are validated directly; part/supplier/SKU lookup must resolve to exactly one supplier-part link. |
| `reference`, `supplier_reference`, `creation_date`, `start_date`, `target_date`, `destination_id` | purchase-order tools | `reference` is InvenTree's internal order identity and is generated when omitted. The combined workflow uses the exact `(supplier_id, supplier_reference)` pair as its retry identity. Dates use `YYYY-MM-DD`; destination is an existing stock-location ID. |
| `start_date_after`, `start_date_before`, `target_date_after`, `target_date_before` | `search_purchase_orders` | Optional schema-backed order date bounds. `status` and exact `reference` filters are also supported. |
| `order_id`, `pending`, `received` | purchase-order line tools | Stable purchase-order filter/target and optional schema-backed line state filters. |
| `line`, `target_date` | purchase-order line writes | Optional display line number and line-specific delivery date. |
| `extra_lines`, `reference`, `description`, `link`, `notes`, `quantity`, `unit_price`, `currency` | purchase-order extra-line tools and `create_purchase_order_with_lines` | Extra lines preserve non-receivable invoice, surcharge, discount, or supplier-product context. Their required trimmed, case-sensitive `(order_id, reference)` identity is unique for MCP-managed writes and retry recovery. Unit prices are exact signed decimals; zero and negative values are supported. Currency is required whenever a price is supplied. `project_code` is intentionally absent. Returned links omit userinfo, query, and fragment. |
| `auto_pricing`, `merge_items` | purchase-order line API payloads | Client-controlled safety fields sent as `false`: supplied preview pricing is preserved and InvenTree cannot silently merge separately referenced workflow lines. These are not exposed as operator inputs. |
| `supplier_reference` | `create_purchase_order_with_lines` | Required stable supplier or external order identifier. Together with `supplier_id` it is the exact retry identity; derived line references use `<supplier_reference>-<one-based index>`. Limited to 64 characters. |
| `purchase_order`, `failure`, `action`, `message`, `recovery_plan` | `create_purchase_order_with_lines` output | Created/reused order, structured partial-failure boundary, failed action, redacted summary, and concrete recovery instructions. Created IDs and completed `actions` remain available after a later write fails. |
| duplicate derived line reference clarification | `create_purchase_order_with_lines` | Returns both line candidates and instructs the operator to use `update_purchase_order_line` to assign one a unique reference before retrying. All existing-line conflicts are preflighted before any metadata or line PATCH. |
| duplicate extra-line reference clarification | purchase-order extra-line writes | Returns stable candidates and refuses creation or movement until one unique trimmed reference remains within the target purchase order. Ambiguous response loss is recovered only when one exact reference match also matches every requested field. |
| `omitted_recommended_fields` | workflow outputs | Recommended fields the caller did not provide, such as IPN, units, purchaseability, default location, supplier SKU, or MPN. |
| `mode` | download tools | Optional download mode. `original` is the default; `thumbnail` is supported for generic attachment downloads when metadata exposes a thumbnail URL and for part-image downloads through `/api/part/thumbs/{id}/`. |
| `max_bytes` | download tools | Optional maximum response content size. Defaults to `5242880` and is capped at `26214400`. |
| `inline_base64` | `upload_attachment`, `set_company_image` | Base64-encoded bytes for an inline upload source. Mutually exclusive with `local_path`. Company images must additionally pass the fixed raster policy. |
| `local_path` | `upload_attachment`, `set_company_image` | STDIO-only local file path under a configured upload allowlist. Mutually exclusive with `inline_base64`. |
| `url` | URL upload and link tools | HTTP(S) URL. `upload_attachment_from_url` and `set_company_image_from_url` fetch bytes; `create_link_attachment` stores the link without fetching. |
| `filename`, `content_type`, `comment`, `tags` | attachment write tools | Optional or required attachment metadata, preserving explicit empty values on metadata updates. For `create_link_attachment`, `filename` is duplicate-preflight-only because InvenTree assigns stored-link filename metadata. |
| `allow_duplicate` | attachment create tools | Explicit intent to add a matching filename, size, or link duplicate after duplicate preflight. |
| `confirm`, `plan_hash` | destructive tools | `confirm:true` is required before attachment, parameter-row, or unreferenced-template deletion. Template merge additionally requires the exact current-state `plan_hash` from `dry_run:true`. |
| `source_template_id`, `target_template_id`, `value_map` | `merge_parameter_templates` | Stable source and target templates plus an optional exact source-to-target value normalization map. Unmapped values are preserved; conflicting rows are skipped. |
| `selection_list_id`, `clear_selection_list`, `references`, `parameter_ids`, `category_link_ids`, `category_links`, `source_template`, `target_template`, `actions`, `model_type`, `model_id`, `part_id`, `from_value`, `to_value`, `source_deleted`, `readback_verified`, `residual_decisions` | parameter-template administration | Explicit nullable selection-list updates, stable reference/preflight evidence, per-object merge actions, and final source deletion/read-back/manual-decision results. Non-part actions omit `part_id` and retain their actual `model_type` and `model_id`; category links include link and category IDs. |
| `part_id` | `set_primary_image` | Stable part primary key that will receive the primary image. |
| `attachment_id` | `set_primary_image` | Stable image attachment primary key already attached to the same part. |
| `source_kind` | attachment write outputs | Source classification such as `inline`, `local_path`, `url`, or `link`. |
| `image_url`, `replaced` | `set_primary_image` output | Redacted resulting image URL and whether an existing primary image was replaced. |
| `company_id` | company-image tools | Stable company primary key. Supplier, manufacturer, customer-only, and mixed-role companies are supported without changing any role. |
| `has_image`, `width`, `height`, `size`, `sha256`, `verified`, `recovered` | company-image output | Explicit image-presence state, decoded dimensions, encoded byte count, exact read-back digest, verification state, and whether an ambiguous response was successfully recovered. Unproved partial results keep `recovered:false`. Image bytes are never returned. |

## Upload Source Resolver

`internal/upload` owns upload source acquisition for attachment write tools and any upload step that prepares a primary-image candidate. It resolves three source kinds before any InvenTree multipart upload code receives bytes:

| Source | Current behavior |
| --- | --- |
| Inline bytes | Copies caller-provided bytes, cleans the supplied filename, preserves the supplied content type, and rejects content above the configured maximum size. |
| STDIO local path | Available only when the caller's mode is STDIO. It uses direct Afero access in `internal/upload/local_file.go`, canonicalizes allowlisted roots and requested paths, rejects paths outside the allowlist, resolves symlink escapes on `OsFs`, opens only after policy checks, and rejects non-regular files. Allowlisted roots must be trusted operator-controlled paths; `OsFs` still has a residual OS-level race if an untrusted user can swap files between policy checks and open. |
| URL fetch | Accepts only HTTP(S) URLs without userinfo, resolves hostnames before fetch and every redirect, blocks local/private/link-local/multicast/reserved/documentation/cloud-metadata-style address ranges by default, caps redirects, does not forward MCP or InvenTree authorization headers, and enforces the configured maximum size and timeout. Private/internal URL targets require an explicit normalized scheme/host/port allowlist entry. |

The resolver returns in-memory content, filename, content type, size, and source kind. Registered attachment and company-image tools are responsible for target validation, multipart upload, tool annotations, and OAuth scope registration. Company-image assignment adds a fixed raster gate: PNG, JPEG, or WebP; matching decoded format, extension, and media type; at most 5 MiB encoded; no dimension above 4096 pixels; and no more than 16 megapixels.

Structured tool outputs must include `status`. Successful calls use `ok`; absent stable records use `not_found`; missing part primary images and already-empty company primary images use `no_image`; ambiguous inputs use `clarification_required`; definite upstream validation rejections use `validation_failed`; and workflows with earlier or ambiguously applied writes use `partial_failure`.

Definite InvenTree validation rejections may include a `validation` object with numeric `status_code` and bounded `fields`. Each field entry contains an allowlisted `field` and canonical `messages`. Messages are classified into safe validation categories instead of echoing raw upstream text, submitted values, URLs, tokens, or unrelated response-body fields.

Clarification outputs must include:

| Field | Behavior |
| --- | --- |
| `status` | Always `clarification_required`. |
| `question` | Specific operator question to resolve the ambiguity. |
| `field` | Field or relationship that is ambiguous or missing. |
| `reason` | Why the tool cannot safely continue. |
| `candidates` | Candidate records with stable IDs, labels, optional summaries, explicit frontend/API links, and extra fields needed for the operator decision. |
| `retry` | Stable field the caller should provide on retry, such as `part_id`, `company_id`, `location_id`, `template_id`, or `attachment_id`. |
| `retry_tool` | Optional destination tool when resolution intentionally crosses tool boundaries; `retry` remains that tool's stable input field. Omitted means retry the original tool. |
| `hard_error` | Whether the API would reject the request, distinct from a recommended-field warning. |
| `retry_values` | Optional non-sensitive prior input values that should be preserved on retry. |

Clarification `candidates` entries use:

| Field | Behavior |
| --- | --- |
| `id` | Stable object ID to provide on retry. |
| `label` | Human-readable object name or identifier. |
| `summary` | Optional short disambiguating detail. |
| `web_url` | Optional absolute URL for the candidate's stable InvenTree frontend page, built only from trusted process configuration. |
| `parent_web_url` | Optional absolute immediate-owner page when the candidate has no stable dedicated frontend route. |
| `api_url` | Optional sanitized relative InvenTree REST inspection path. This clarification-only field does not carry an authority. |
| `fields` | Optional non-sensitive structured details needed for the decision. |

The former ambiguous clarification-candidate `url` field is removed as an intentional breaking change. Normal object projections use `web_url` for their stable frontend page; subordinate projections without one omit it and use `parent_web_url` only when their existing relationship fields identify an immediate owner with a stable page. See [User-facing InvenTree web links](web-links.md) for the pinned route matrix and complete output classification.

The registered-tools table below summarizes the current server surface and preserves each tool's milestone status in `docs/tool-manifest.json`, the checked machine-readable source for mutation classes, scopes, annotations, upload sources, and HTTP registration state.

## Registered Lookup Tools

All tools in this section are implemented and registered with status `milestone_1` in the checked manifest. They use class `read_only`, MCP annotations `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, and `openWorldHint:false`. They require OAuth scope `inventree.read` when OAuth authorization mode is enabled. Upload sources are `None`.

| Tool | Group | Inputs | Output | Ask operator when |
| --- | --- | --- | --- | --- |
| `search_parts` | Part lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `part_id` | Search returns multiple plausible parts. |
| `get_part` | Part lookup | `id` | `status`, `record` | Provided ID does not exist. |
| `search_part_categories` | Part lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `category_id` | Category path/name is ambiguous. |
| `get_part_category` | Category administration | `id` | `status`, exact `record` with hierarchy and default metadata | Stable category ID does not exist. |
| `search_parameter_templates` | Parameters | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `template_id` | Same-name templates differ by unit, choices, checkbox behavior, or category link. |
| `get_part_parameters` | Parameters | `part_id`, `limit`, `offset` | `status`, `count`, `results` | Part ID is missing or ambiguous. |
| `search_part_parameters` | Parameters | at least one of `template_id` or exact `template_name`, exact `value`, `category_id`, or `part_id`; optional `limit`, `offset` | `status`, `count`, `results` with stable `parameter_id`, part/category/template IDs, template metadata, and value; optional `clarification` with retry filters or `template_id` | No narrowing filter is supplied, exact template name is missing or ambiguous, the name conflicts with `template_id`, the requested offset/limit window exceeds 1,000 rows, or complete row-ID ordering cannot be established within the 1,000-row scan bound. |
| `search_category_parameter_defaults` | Category defaults | optional `category_id`, `template_id`, `include_parent_defaults`, `limit`, `offset` | `status`, `count`, `has_more`, enriched direct-link `records` with stable link/category/template IDs, source names, default values, requested category, and inherited flag | IDs are invalid, inheritance is requested without a category, or a filtered scan exceeds 1,000 links. Exact-category results are the default. |
| `audit_parameter_consistency` | Parameter audit | optional `template_id`, exact `category_id` | bounded read counts plus ordered findings for duplicate names/rows/defaults, incompatible definitions, overloaded same-name fields, unlinked use, and category-default mismatches | An ID is invalid or the combined upstream request-and-record workload exceeds the 1,000-unit budget. Template filters include same-normalized-name peers; category filters narrow parts and direct links before row scanning. The audit never writes. |
| `search_companies` | Company lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `company_id` | Supplier/manufacturer identity is ambiguous. |
| `search_suppliers` | Company lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `supplier_id` | Supplier role is unclear. |
| `search_manufacturers` | Company lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `manufacturer_id` | Manufacturer role is unclear. |
| `get_company` | Company administration | stable `id` | approved company fields including customer-role context and notes; website credentials, query, and fragment are stripped | Stable company ID is invalid or unreadable. |
| `search_supplier_parts` | Sourcing links | optional `search`, `part_id`, `supplier_id`, exact `sku`, `limit`, `offset` | one complete bounded snapshot of stable identity/state projections, sorted by ID before applying the requested window | Filters or pagination are invalid, the requested window exceeds 1,000, the matching set exceeds 1,000, or a later write still has duplicate ambiguity. |
| `get_supplier_part` | Sourcing links | stable `id` | approved exact supplier-part fields with a redacted link URL | Stable supplier-part ID is invalid or unreadable. |
| `search_manufacturer_parts` | Sourcing links | optional `search`, `part_id`, `manufacturer_id`, exact `mpn`, `limit`, `offset` | one complete bounded snapshot of stable identity projections, sorted by ID before applying the requested window | Filters or pagination are invalid, the requested window exceeds 1,000, the matching set exceeds 1,000, or a later write still has duplicate ambiguity. |
| `get_manufacturer_part` | Sourcing links | stable `id` | approved exact manufacturer-part fields with a redacted link URL | Stable manufacturer-part ID is invalid or unreadable. |
| `search_stock_locations` | Stock lookup | `search`, `limit`, `offset` | `status`, `count`, `results`, optional `clarification` with retry `location_id` | Location name/path is ambiguous. |
| `get_stock_location` | Stock administration | stable `id` | exact hierarchy, parent/path, owner, structural/external, item/child counts, custom icon, and location-type context | Stable location ID is invalid or unreadable. |
| `search_stock_location_types` | Stock administration | `search`, `limit`, `offset` | existing location-type stable IDs, names, descriptions, icons, and location counts | The intended existing location type is ambiguous. Location-type mutation is not exposed. |
| `get_stock_location_type` | Stock administration | stable `id` | exact existing location-type record | Stable location-type ID is invalid or unreadable. |
| `search_stock_items` | Stock lookup and receipt recovery | `search`, `part_id`, `location_id`, `purchase_order_id`, `limit`, `offset` | `status`, `count`, `results` including source `purchase_order` and `purchase_order_reference` | Existing stock may duplicate requested initial stock, or a non-idempotent purchase receipt needs source-order readback before retry. |
| `get_stock_item` | Stock administration | stable `id` | exact quantity, status, location/path, serial, batch, expiry, packaging, notes, allocation, ownership, installation, and source-order/build context; external-link credentials, query, and fragment are stripped | Stable stock-item ID is invalid or unreadable. |
| `list_attachments` | Attachments | `model_type`, `model_id`, `search`, `limit`, `offset` | `status`, `count`, `results` | Target object is ambiguous. |
| `get_attachment_metadata` | Attachments | `id` | `status`, `record` | Attachment ID is missing or ambiguous. |
| `download_attachment` | Attachments | `id`, `mode`, `max_bytes` | `status`, `id`, `filename`, `content_type`, `size`, `sha256`, `mode`, `source_url`, plus `text` or `base64` content | Operator may mean stored-link metadata versus an external link target, or original file versus explicit thumbnail mode. |
| `download_part_image` | Attachments | `id`, `mode`, `max_bytes` | `status`, `id`, `filename`, `content_type`, `size`, `sha256`, `mode`, `source_url`, plus `text` or `base64` content | Operator may mean a generic attachment rather than the current primary image, or original image versus explicit thumbnail mode. |
| `preview_purchase_order_with_lines` | Purchasing preview | `supplier_id`, `lines` with `supplier_part_id` or `part_id` plus supplier context, `quantity`, optional `unit_price`, `currency`, `notes` | `status`, `supplier_id`, `lines`, optional `warnings`, optional `clarification` | Supplier part, supplier, part, quantity, or price currency is ambiguous. |
| `search_purchase_orders` | Purchasing recovery | `search`, `supplier_id`, exact `reference`, optional `status`, date bounds, `limit`, `offset` | `status`, `count`, `results` | Search filters do not identify the intended order. |
| `get_purchase_order` | Purchasing recovery | `id` | `status`, `record` | Stable purchase-order ID does not exist. |
| `search_purchase_order_lines` | Purchasing recovery | `search`, `order_id`, `supplier_part_id`, optional `pending`, `received`, `limit`, `offset` | `status`, `count`, `results` | Order or supplier-part identity is unclear. |
| `get_purchase_order_line` | Purchasing recovery | `id` | `status`, `record` | Stable purchase-order-line ID does not exist. |
| `search_purchase_order_extra_lines` | Purchasing extra-line recovery | optional `search`, `order_id`, `limit`, `offset` | `status`, total `count`, bounded page `results` with sanitized links | Purchase-order identity is unclear or a bounded result cannot be established. |
| `get_purchase_order_extra_line` | Purchasing extra-line recovery | stable `id` | `status`, exact stable-ID `record` with sanitized link | Stable extra-line ID does not exist or read-back identity diverges. |

## Registered Write Tools

`update_company`, `update_supplier_part`, `update_manufacturer_part`, `update_stock_location`, `restructure_stock_location`, `update_stock_item_metadata`, and `update_purchase_order_extra_line` use `idempotentHint:true` for absolute stable-ID PATCH values. Each still performs fresh preflight and read-back because the upstream checks and mutation are not transactional.

The tools in this section are implemented and registered only when write tools are explicitly enabled by the server dependency configuration. The current CLI enables them for STDIO mode. HTTP registration requires OAuth authorization mode so every call passes through the per-tool scope guard before the handler runs.

Tools in this section record status `milestone_1` in the checked manifest. Ordinary write tools use mutation class `write` with MCP annotations `readOnlyHint:false`, `destructiveHint:false`, and `openWorldHint:false`; most use `idempotentHint:false`. Absolute stable-ID location metadata, hierarchy, and stock-metadata PATCH tools use `idempotentHint:true`; hierarchy and stock-metadata changes use mutation class `operational`. The combined purchase-order workflow and bulk parameter propagation remain `idempotentHint:false` because writes and recovery reads are not atomic. Stock creation, stock adjustments, location restructuring, constrained stock-metadata updates, and purchase receiving use mutation class `operational`. `upload_attachment_from_url` and `set_company_image_from_url` use `openWorldHint:true`; attachment/parameter/category-default deletion, company-image clear, parameter-template merge, bulk parameter propagation, and intentional delete-on-deplete stock removal use mutation class `destructive` with `destructiveHint:true`. Most tools require OAuth scope `inventree.write` when OAuth authorization mode is enabled. Category, template, category-default, and stock-location create/update require `inventree.read` because collision, reference, hierarchy, and recovery preflight can disclose existing records. Initial stock creation requires `inventree.write` and `inventree.operational`; current-state-bound stock adjustments, location restructuring, stock-metadata updates, and purchase receiving additionally require `inventree.read` for preflight and unknown-result recovery. `deplete_stock_item` additionally requires both `inventree.operational` and `inventree.destructive`. Attachment tools require `inventree.write` and `inventree.upload`; attachment delete also requires `inventree.destructive`. Company-image assignment additionally requires `inventree.read` for exact content and unrelated-field verification; clear also requires `inventree.destructive`. Parameter-row, unreferenced-template, and category-default deletion, template merge, and bulk parameter propagation require `inventree.read`, `inventree.write`, and `inventree.destructive` so they can preflight references or identities, present current records or plans, verify mutations, and authorize value replacement.

| Tool | Group | Inputs | Output | Ask operator when |
| --- | --- | --- | --- | --- |
| `create_part` | Part entry | `name`, `category_id`, optional `description`, `ipn`, `units`, `active`, `assembly`, `component`, `purchaseable`, `trackable`, `virtual`, `default_location_id` | `status`, `record`, optional `clarification` with retry `part_id`, `category_id`, or `default_location_id` | Category ID or default location ID is missing/invalid, or matching parts already exist. |
| `update_part` | Part entry | `id`, optional `name`, `description`, `category_id`, `ipn`, `units`, `active`, `assembly`, `component`, `purchaseable`, `trackable`, `virtual`, `default_location_id` | `status`, `record`, optional `clarification` with retry `id`, `category_id`, or `default_location_id` | Part ID or referenced IDs are invalid, caller provides names instead of stable IDs, or no PATCH fields are supplied. |
| `delete_part` | Part entry | stable `id`, `confirm` | previewed `record`, `category`, `supplier_parts`, `manufacturer_parts`, `parameters`, `attachments`, `related_parts`, then one of four states: (1) `status:"ok"`, `verified:true`, `recovered` omitted (false) for an ordinary confirmed deletion; (2) `status:"ok"`, `verified:true`, `recovered:true` when the `DeletePart` call itself returned an ambiguous error (a lost/dropped response, distinct from a definite upstream rejection) but read-back proves the deletion applied anyway; (3) `status:"partial_failure"` with `record` and a recovery plan when read-back finds the part still exists after a delete attempt; (4) `status:"partial_failure"` when the read-back itself cannot prove either outcome; or `blocking` with stable IDs by category when refused before ever calling delete | Stable identity or explicit confirmation is missing, the part is still active, or stock, bill-of-materials (as assembly or as a component elsewhere), builds, purchase-order lines, sales-order lines, variant parts, supplier parts, manufacturer parts, parameters, attachments, or related-part links still reference this part. Pinned InvenTree 1.4.3 itself enforces only two of these (active-state, and use as a component in another part's BOM); every other category is silently permitted once the part is inactive, and in the case of stock the referencing record is destroyed along with the part rather than merely orphaned, so this tool refuses on every category itself rather than relying on upstream. A definite mutation rejection (a real 4xx status) is returned directly; an ambiguous mutation failure (timeout, 5xx, dropped connection, or a retryable 408/425/429) is instead verified by read-back rather than assumed to have failed. Requires destructive scope. |
| `create_part_category` | Category administration | required trimmed `name`; optional `description`, `parent_id`, `default_location_id`, `default_keywords`, `structural`, `icon` | exact read-back `record`, optional recovered stable ID and `recovery_plan`, or duplicate clarification | References are invalid, a normalized same-parent duplicate exists, or bounded pagination cannot prove uniqueness. |
| `update_part_category` | Category administration | stable `id`; optional writable fields and explicit nullable clear flags; `confirm:true` for reparenting or structural change | `before`, exact read-back `record`, hierarchy context, recovery guidance, or clarification | Patch/reference/duplicate/cycle/structural policy is invalid, or required confirmation is missing. |
| `set_part_parameters` | Parameters | `part_id`, `parameters` array with `template_id` or `name`, and exactly one of `value`, `bool_value`, or `number_value` | `status`, `record[]`, optional `clarification` with retry `template_id`, `parameter_id`, `category_id`, or `value` | Template name is ambiguous, matching templates differ by unit/choices/checkbox behavior, the template is disabled, the template is not linked to the part category, multiple existing part parameters use the same template, or creating a new template/category link would be required. |
| `delete_part_parameter` | Parameters | stable `parameter_id`, `confirm` | `status`, deleted row `record`, `verified`, optional `clarification` | Stable row ID is missing, the row is not a part parameter, or `confirm:true` is missing after candidate review. Bulk and whole-template deletion are refused. |
| `create_parameter_template` | Parameter templates | explicit `name`, `units`, `description`, schema-enum `model_type` (or empty), `checkbox`, `choices`, and `enabled`; optional `selection_list_id` | `status`, created `record`, optional `clarification` | Any explicit field is omitted, the model type or selection-list ID is invalid, or a case-insensitive same-name template already exists. |
| `update_parameter_template` | Parameter templates | stable `template_id` plus one or more optional replacement fields; `clear_selection_list` explicitly writes null | `status`, updated `record`, optional `clarification` | No field is supplied, the name is blank/colliding, or selection-list instructions conflict. Omitted fields remain unchanged. |
| `delete_parameter_template` | Parameter templates | stable `template_id`, `confirm` | `status`, template `record`, parameter/category-link reference IDs, `verified`, optional `clarification` | Any parameter row or category-default link still references the template, or confirmation is missing. Referenced templates never cascade-delete. |
| `merge_parameter_templates` | Parameter templates | stable source/target IDs, optional exact `value_map`, `dry_run`, `confirm`, current `plan_hash` | source/target records, per-object row actions, structured category-link references, residual decisions, deletion/read-back verification | IDs are invalid, the plan changed, a part already has both rows, a non-part row or category-default link needs manual handling, or confirmation is missing. Non-conflicting part rows move; the source is deleted only after fresh empty/unlinked preflight. |
| `create_category_parameter_default` | Category defaults | existing `category_id`, existing `template_id`, explicit `default_value` | `status`, enriched direct-link `record`, optional `clarification` | IDs are invalid, default exceeds 500 characters, or the exact category/template link already exists. |
| `update_category_parameter_default` | Category defaults | stable direct `link_id` plus optional `category_id`, `template_id`, or `default_value` | `status`, enriched direct-link `record`, optional `clarification` | No field is supplied, an ID is invalid, or the resulting exact category/template pair duplicates another link. |
| `delete_category_parameter_default` | Category defaults | stable direct `link_id`, `confirm` | `status`, source-category link snapshot, `verified`, optional `clarification` | Link ID is invalid or confirmation is missing. Inherited results must be mutated through the source link ID shown by search. |
| `bulk_propagate_part_parameters` | Parameter propagation | one enabled part-compatible `template_id`, explicit `value`, exactly one of at most 100 `part_ids` or `category_id`, optional `include_subcategories`, `overwrite_existing`, plus `dry_run`, `confirm`, `plan_hash` | current-state-bound ordered actions, applied/skipped/failed/manual counts, read-back verification, and recovery details | The selector is missing/broad, a template/category/part is invalid, the template is unlinked, existing values differ without overwrite approval, duplicate rows exist, state changed, or confirmation is missing/stale. The tool never creates templates/links or deletes rows. |
| `create_company` | Company entry | `name`, `currency`, at least one of `is_supplier` or `is_manufacturer`, optional `description`, `website` | `status`, `record`, optional `clarification` with retry `company_id`, `currency`, or `is_supplier` | Matching companies already exist, currency is missing, no supported role is selected, or the caller asks for a customer/sales workflow. |
| `create_supplier_part` | Supplier link | `part_id`, `supplier_id`, `sku`, optional `description`, `link`, `active`, `primary`, `manufacturer_part_id`, `packaging`, `note` | `status`, `record`, optional `clarification` with retry `supplier_part_id`, `part_id`, `supplier_id`, or `manufacturer_part_id` | Part, supplier, or manufacturer-part ID is invalid, or matching supplier-part links already exist. |
| `create_manufacturer_part` | Manufacturer link | `part_id`, `manufacturer_id`, optional `mpn`, `description`, `link` | `status`, `record`, optional `validation` or `clarification` with retry `manufacturer_part_id`, `part_id`, or `manufacturer_id` | Part or manufacturer ID is invalid, or matching manufacturer-part links already exist. Blank optional MPN input is omitted rather than sent or replaced with an invented value. Pinned InvenTree 1.4.3 rejects that schema-optional omission, so direct creation without an MPN returns safe `validation_failed` output. |
| `update_company` | Company administration | stable `id`; optional approved absolute fields; `clear_notes`; `confirm:true` when removing supplier/manufacturer role | exact read-back `record`, minimal `before`, minimal recovered `current`, or read-before-retry plan | Patch is empty/invalid, role removal is unconfirmed, or corresponding sourcing links still exist. Customer/contact/tax/tag/image fields are not accepted. |
| `update_supplier_part` | Sourcing links | stable `id`; optional part, supplier, SKU, description, link, active/primary, manufacturer-part, packaging, pack quantity, short note, and explicit clear flags | exact read-back `record`, minimal `before`/recovered `current`, minimal duplicate `candidates`, or read-before-retry plan | References are invalid, supplier lacks its role, normalized supplier+SKU duplicates another link, or postflight cannot prove role/reference integrity and uniqueness. |
| `update_manufacturer_part` | Sourcing links | stable `id`; optional part, manufacturer, MPN, description, link, and explicit clear flags | exact read-back `record`, minimal `before`/recovered `current`, minimal duplicate `candidates`, minimal `blocking_supplier_parts`, or read-before-retry plan | References are invalid, manufacturer lacks its role, changing the base part conflicts with linked supplier parts, normalized manufacturer+MPN duplicates another link, or postflight cannot prove role/reference integrity and uniqueness. |
| `upsert_part_with_supplier_and_manufacturer` | Part workflow | `dry_run`, `part_id` or `name`, optional part fields, optional supplier/manufacturer IDs or names, required `supplier_sku` when linking a supplier, optional `mpn`, and currencies when creating companies | `status`, `dry_run`, `actions`, dry-run `planned_changes`, factual selected/reused/created `part`, `supplier`, `manufacturer`, `supplier_part`, `manufacturer_part` when stable, `omitted_recommended_fields`, `remaining_actions`, optional `failure` with nested `validation`, or `clarification` | Part, supplier, manufacturer, supplier-part, or manufacturer-part matches are ambiguous; category or currency is missing before creation; supplier SKU is missing before linking. Blank optional MPN input is omitted; one existing part/manufacturer link is reused, multiple links require clarification, and no link produces a skipped manufacturer-part action while the supplier-part path continues. A later failure preserves completed actions, stable IDs, remaining work, and recovery guidance. |
| `create_stock_item` | Initial stock | `part_id`, `location_id`, `quantity`, optional `status`, `batch`, `serial`, `notes` | `status`, `record`, optional `clarification` with retry `stock_item_id`, `part_id`, `location_id`, `quantity`, or `status` | Part, location, quantity, or status is invalid, or existing stock already matches the requested part and location. |
| `create_initial_stock_entry` | Initial stock workflow | `dry_run`, `part_id` or `part_search`, `location_id` or `location_search`, `quantity`, optional `status`, `batch`, `serial`, `notes` | `status`, `dry_run`, `actions`, dry-run `planned_changes`, selected `part`, selected `location`, optional created `stock_item`, optional `clarification` | Part or location search is ambiguous, quantity/status is invalid, or existing stock already matches the requested part and location. |
| `create_stock_location` | Stock administration | required trimmed `name`; optional `description`, `parent_id`, `owner_id`, `custom_icon`, `structural`, `external`, `location_type_id` | exact read-back record, recovered stable ID after ambiguous response, or duplicate/reference clarification | A reference is invalid, normalized same-parent identity exists, or a complete duplicate scan exceeds 1,000 records. |
| `update_stock_location` | Stock administration | stable `id`; optional name/description/owner/custom-icon/location-type fields and explicit nullable clear flags | exact read-back record or recovery guidance | The ordinary patch is empty/invalid, a reference or duplicate is unresolved, or read-back diverges. Parent, structural, and external fields are intentionally absent. |
| `restructure_stock_location` | Stock hierarchy | stable `id`; parent replacement/clear, structural and/or external state; `dry_run`, `confirm`, exact `plan_hash` | current-state before/after hierarchy plan and exact read-back record | Parent is invalid/self/descendant, the resulting identity collides, state changed, or reviewed confirmation is missing. |
| `update_stock_item_metadata` | Stock metadata | stable `id`; only batch, expiry date, packaging, notes, HTTP(S) link and explicit nullable clear flags; `dry_run`, `confirm`, exact `plan_hash` | complete bound metadata/traceability plan and exact read-back record | Patch is empty/invalid, state changed, or reviewed confirmation is missing. Location, quantity, status, serial, ownership, source/pricing, deletion, installation, and order/build fields are absent. |
| `adjust_stock_quantity` | Stock adjustment | `dry_run`, `stock_item_id`, non-zero `delta`, nonblank `reason`, `confirm`, opaque `plan_hash` token | `status`, `dry_run`, before/after `plan`, `plan_hash`, refreshed `record`, optional `failure` or `clarification` | Identity, quantity, reason, confirmation, or token is invalid; negative quantity, serialized stock, no-op, or implicit `delete_on_deplete` deletion is refused. Decreases are high-risk. |
| `set_stock_status` | Stock status | `dry_run`, `stock_item_id`, schema-supported `status`, nonblank `reason`, `confirm`, opaque `plan_hash` token | `status`, `dry_run`, before/after `plan`, `plan_hash`, refreshed `record`, optional `failure` or `clarification` | Identity, status, reason, confirmation, or token is invalid. No-op changes are refused. `Destroyed`, `Rejected`, and `Lost` are high-risk. |
| `stocktake_adjustment` | Stocktake | `dry_run`, `stock_item_id`, non-negative `observed_quantity`, nonblank `reason`, `confirm`, opaque `plan_hash` token | `status`, `dry_run`, before/after `plan`, `plan_hash`, refreshed `record`, optional `failure` or `clarification` | Identity, observed quantity, reason, confirmation, or token is invalid. Serialized stock, no-op, and implicit `delete_on_deplete` deletion are refused. A lower count is high-risk; quantity is the only changed field. |
| `deplete_stock_item` | Stock depletion | `dry_run`, stable `stock_item_id`, nonblank `reason`, `confirm`, opaque `plan_hash` token | high-risk current-state `plan` with `will_delete:true`, safe relationship/provenance context, `verified`, optional `recovered`, current `record`, `failure`, or `clarification` | Identity, reason, confirmation, or token is invalid; quantity is not positive; `delete_on_deplete` is false; or stock is unavailable, allocated, serialized, building, consumed, installed, parent-linked, or has installed/child items. The tool removes the complete current quantity and verifies exact-ID absence. |
| `transfer_stock_item` | Stock transfer | `dry_run`, stable `stock_item_id`, explicit stable `destination_location_id`, nonblank `reason`, `confirm`, opaque `plan_hash` token; no quantity input | current-state before/after `plan`, source/destination paths, safety and provenance context, `will_split:false`, exact refreshed `record`, `verified`, optional `recovered`, `failure`, or `clarification` | Identity, current source location, destination, reason, confirmation, or token is invalid; destination is unchanged; or source stock is unavailable, allocated, serialized, build/consumption/installation/customer/sales-linked, parent-linked, or contains installed items or child stock. Audit tracking history is reported by InvenTree but is not a child-stock relationship or a transfer blocker. Every exact-read destination is submitted to native InvenTree validation without MCP structural, external, or ownership exclusions. The complete current quantity moves and the original stable ID is verified at the destination. |
| `create_purchase_order` | Purchasing | `supplier_id`, optional pattern-compliant `reference`, `supplier_reference`, `description`, `creation_date`, `start_date`, `target_date`, `currency`, `destination_id` | `status`, `record`, optional `clarification` | Supplier is invalid or lacks the supplier role, a supplied reference already exists, dates are malformed, or destination is invalid. InvenTree generates `reference` when omitted. This one-shot tool may omit `supplier_reference`, but only the combined workflow provides recovery after a client or tool interruption. |
| `add_purchase_order_line` | Purchasing | `order_id`, `supplier_part_id`, `quantity`, optional `line`, `reference`, `notes`, `unit_price`, `currency`, `target_date`, `destination_id` | `status`, `record`, optional `clarification` | Supplier part does not belong to the order supplier, quantity is invalid, or priced line lacks currency. |
| `update_purchase_order_line` | Purchasing | `id` plus optional `supplier_part_id`, `quantity`, `line`, `reference`, `notes`, `unit_price`, `currency`, `target_date`, `destination_id` | `status`, `record`, optional `clarification` | No PATCH fields are supplied or the replacement supplier part does not belong to the order supplier. |
| `delete_purchase_order_line` | Purchasing | stable `id`, `confirm` | previewed `record`, `purchase_order`, resolved `part_id`, then `status`, deleted snapshot, refreshed `purchase_order`, and `verified:true` only after not-found read-back | Stable identity or explicit confirmation is missing, the line already has received quantity, or stock items already reference this order and supplier part. InvenTree's DELETE endpoint neither restricts by order status nor rejects a line with received quantity, so this tool refuses both cases itself before ever calling delete; never deletes or adjusts received stock. Requires destructive scope. Distinct from `delete_purchase_order_extra_line`, which only removes non-receivable extra lines. |
| `create_purchase_order_extra_line` | Purchasing extra line | `dry_run`, stable `order_id`, required unique `reference`, nonnegative `quantity`, optional description/line/link/notes, exact signed `unit_price` with `currency`, and `target_date` | `status`, `dry_run`, exact `record`, refreshed `purchase_order` and `affected_purchase_orders` including server `total_price`, field-level `planned_changes`, verification/recovery state, validation or clarification | Target order/reference is invalid, one or more reference matches conflict, or exact response-loss recovery cannot be proved. |
| `update_purchase_order_extra_line` | Purchasing extra line | `dry_run`, stable `id`, optional replacement order/reference/description/line/link/notes/quantity/price/currency/date, plus explicit nullable price/date clears | `status`, `dry_run`, exact `record`, refreshed destination `purchase_order`, every refreshed source/destination order in `affected_purchase_orders`, field-level `planned_changes`, verification/recovery state, validation or clarification | Patch is empty, target order/reference conflicts, or exact read-back cannot be proved. |
| `delete_purchase_order_extra_line` | Purchasing extra line | stable `id`, `confirm` | previewed `record` and purchase order, then `status`, deleted snapshot, refreshed `purchase_order` and `affected_purchase_orders`, and `verified:true` only after not-found read-back | Stable identity or explicit confirmation is missing. Requires destructive scope. |
| `create_purchase_order_with_lines` | Purchasing workflow | `dry_run`, `supplier_id`, required stable `supplier_reference`, optional `purchase_order_id` ambiguity resolution, optional order metadata, receivable `lines`, and non-receivable `extra_lines` with unique references and signed prices | `status`, `dry_run`, `supplier_reference`, factual `purchase_order`, `lines`, and `extra_lines`, ordered `actions`, dry-run `planned_changes`, optional `failure` or `clarification` | Supplier/order/line/extra-line identity conflicts, preview validation fails, or exact recovery is ambiguous. Normal receivable lines execute before extra lines; accumulated IDs/actions remain on partial failure. The workflow is retry-recoverable but retains `idempotentHint:false` because duplicate lookup and creation are not atomic. |
| `issue_purchase_order` | Purchasing | `dry_run`, `order_id`, `confirm_issue`, exact dry-run `plan_hash` | `status`, `dry_run`, factual `order`, current receivable `lines`, current non-receivable `extra_lines`, `action`, dry-run `planned_changes` with the target `PLACED` status, current-state `plan_hash`, optional `clarification` | Order is not pending, placing it lacks `confirm_issue:true`, or the hash no longer matches current order metadata and receivable-line or extra-line identities, quantities, prices, destinations, dates, notes, or receipt state. Already-placed orders return `already_placed` without mutation. |
| `receive_purchase_order_items` | Purchasing receipt | `dry_run`, `order_id`, optional global `location_id`, `confirm_receive`, exact dry-run `plan_hash`, and `items` with stable `line_item_id`, schema-valid partial `quantity`, optional item location, status, batch, expiry, serials, packaging, note, and barcode | `status`, `dry_run`, refreshed `order`, resolved `plan` including supplier-pack/base-stock quantities and resolved packaging, dry-run `plan_hash`, created `stock_items`, optional `failure` or `clarification` | Order is not placed; a line belongs to another order; part is virtual; quantity exceeds outstanding or InvenTree decimal bounds; location, status, expiry, or barcode is invalid; or execution lacks matching dry-run confirmation. Blank or whitespace-only packaging does not clear packaging; it uses the supplier-part default. Creates new stock only and never merges or updates existing stock. Definite 4xx rejections remain actionable errors; ambiguous writes return `partial_failure` with `purchase_order_id` read-before-retry recovery guidance. |
| `upload_attachment` | Attachments | `model_type`, `model_id`, `filename`, `content_type`, exactly one of `inline_base64` or `local_path`, optional `comment`, `tags`, `allow_duplicate` | `status`, `record`, `source_kind`, optional `clarification` with retry `inline_base64`, `filename`, `content_type`, `model_id`, `url`, or `allow_duplicate` | Target object is out of scope, source is missing/ambiguous, filename or content type is missing, local path is not allowlisted, URL intent is ambiguous, or duplicate preflight matches existing attachments. |
| `upload_attachment_from_url` | Attachments | `model_type`, `model_id`, `url`, optional `filename`, `comment`, `tags`, `allow_duplicate` | `status`, `record`, `source_kind`, optional `clarification` with retry `filename`, `model_id`, or `allow_duplicate` | URL policy rejects the target, filename cannot be determined, or duplicate preflight matches existing attachments. |
| `create_link_attachment` | Attachments | `model_type`, `model_id`, `url`, optional duplicate-preflight `filename`, `comment`, `tags`, `allow_duplicate` | `status`, `record`, `source_kind`, optional `clarification` with retry `model_id` or `allow_duplicate` | URL is not HTTP(S), includes credentials or a fragment, target object is out of scope, or duplicate preflight matches an existing link. InvenTree assigns stored-link filename metadata. |
| `update_attachment_metadata` | Attachments | `id`, optional `filename`, `comment`, `tags` | `status`, `record`, optional `clarification` with retry `id` | Stable attachment ID is missing, target object is out of scope, or no PATCH fields are supplied. |
| `delete_attachment` | Attachments | `id`, `confirm` | `status`, `record`, optional `clarification` with retry `confirm` | Stable attachment ID is missing, target object is out of scope, or `confirm:true` is missing. |
| `set_primary_image` | Attachments | `part_id`, `attachment_id`, `confirm` | `status`, `record`, `part_id`, `image_url`, `replaced`, optional `clarification` with retry `attachment_id` or `confirm` | Attachment is not an image file on the requested part, multiple candidate images exist before a stable `attachment_id` is supplied, or replacement lacks `confirm:true`. |
| `set_company_image` | Company images | stable `company_id`, `content_type`, exactly one of `inline_base64` or `local_path`, inline-required or local-optional `filename`, optional `confirm` | `status`, verified `image` metadata including image presence, dimensions, size and SHA-256, `replaced`, `recovered`, `verified`, optional validation, clarification, current state, or recovery plan | Identity or source is unresolved, raster policy rejects the content, replacement lacks confirmation, or exact digest/unrelated-field read-back cannot be proved. |
| `set_company_image_from_url` | Company images | stable `company_id`, HTTP(S) `url`, optional `filename`, optional validated `content_type` override, optional `confirm` | Same verified output as `set_company_image` | URL policy or raster validation rejects the source, replacement lacks confirmation, or exact read-back cannot be proved. |
| `clear_company_image` | Company images | stable `company_id`, `confirm` | `status`, `current.has_image:false` with no image URL, `recovered`, `verified`, optional validation, clarification, or recovery plan | Image is already absent, confirmation is missing, or exact null read-back and unrelated-field preservation cannot be proved. Pinned InvenTree removes the associated stored media file. |

## Skeleton Tools

| Tool | Group | Milestone status | Class | MCP annotations | Scopes | Upload sources | HTTP registration | Ask operator when |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `health_version` | Server health | `milestone_1` | `read_only` | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | None | None | `registered` | Never; returns static server health and build metadata. |

## Registered Prompts

Prompts are static operator checklists registered through the MCP prompt surface. They do not call InvenTree directly and do not replace tool-level clarification responses. Milestone 1 prompts prefer existing records, stable ID retries, dry-run plans, and structured clarification over guessed categories, units, supplier SKUs, manufacturer part numbers, order states, prices, locations, stock status, or quantities.

| Prompt | Milestone status | Purpose | Guardrail |
| --- | --- | --- | --- |
| `category_administration_checklist` | `milestone_1` | Review exact category reads and guarded category creation/editing. | Use stable references, bounded duplicate preflight, cycle safeguards, and explicit confirmation for reparenting or structural-state changes. |
| `new_part_entry_checklist` | `milestone_1` | Add or update a purchasable part with supplier/manufacturer context. | Search existing records first, ask for stable IDs on ambiguity, and prefer `upsert_part_with_supplier_and_manufacturer` with `dry_run:true` before writing. |
| `parameter_reuse_checklist` | `milestone_1` | Reuse existing parameter templates when setting part parameters. | Prefer category-linked templates and ask for `template_id` when same-name templates differ by unit, choices, checkbox behavior, or category link. |
| `parameter_consistency_review` | `milestone_1` | Audit parameter consistency and review bounded single-template propagation. | Keep audits read-only; require explicit selectors, dry-run review, overwrite intent, and a matching five-minute single-use plan before propagation. |
| `attachment_image_checklist` | `milestone_1` | Prepare attachment/image reads, uploads, links, metadata updates, deletes, and primary-image replacement workflows. | Keep upload-copy, stored-link, metadata update, delete, and primary-image replacement intents distinct; require a stable image attachment ID and `confirm:true` before replacement. |
| `initial_stock_entry_checklist` | `milestone_1` | Create initial stock after duplicate preflight. | Resolve stable part/location IDs, require positive quantity, and prefer `create_initial_stock_entry` with `dry_run:true`. |
| `purchase_preview_checklist` | `milestone_1` | Produce no-write purchase-order line previews. | Validate supplier-part identity and positive quantities; never create purchase orders or purchase-order lines. |
| `receive_purchase_order_checklist` | `milestone_1` | Prepare explicit issue and purchase-order receiving workflows. | Use stable IDs and dry runs, distinguish receivable supplier-part lines from non-receivable extra lines, receive only outstanding normal-line quantities, resolve destinations, and require explicit confirmations. |
| `stocktake_review` | `milestone_1` | Review one stock quantity or status adjustment. | Read current state, require an audit reason and dry run, review high-risk decreases/write-offs, and execute only with `confirm:true` plus the unexpired single-use `plan_hash` token from the same principal. Do not adjust one item concurrently. |
| `stock_administration_checklist` | `milestone_1` | Review exact stock reads, location administration, and constrained metadata updates. | Use stable references, bounded same-parent duplicate proof, operational plans for hierarchy/structural/external and stock-metadata changes, and never expose generic stock PATCH, movement, deletion, quantity, status, or serial mutation. |

Future prompts remain in the internal prompt manifest with status `future` and are not registered through MCP until their workflows are implemented:

- `bom_import_review`

Completed F-S03 exposes retry-recoverable creation, explicit confirmed issue, and dry-run/confirmed partial receiving that creates new stock items without merging or updating existing stock. F-S15 completes the order-entry hardening contract: missing MPN is valid, blank optional MPN input is omitted, the combined workflow reuses an unambiguous existing manufacturer-part link or skips creation when none exists before continuing supplier entry, existing lower-level duplicate/reference preflight is retained where a separate dry run adds no safety, validation fields are bounded and canonicalized, and the combined part workflow preserves stable recovery state across later failures.

## Registered Tools

| Tool | Group | Class | Scopes | Upload sources | MCP annotations | HTTP registration | Ask operator when |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `search_parts` | Part lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Search returns multiple plausible parts. |
| `get_part` | Part lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Provided ID does not exist. |
| `search_part_categories` | Part lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Category path/name is ambiguous. |
| `get_part_category` | Category administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable category ID does not exist. |
| `search_parameter_templates` | Parameters | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Same-name templates differ by unit, choices, checkbox behavior, or category link. |
| `get_part_parameters` | Parameters | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Part ID is missing or ambiguous. |
| `search_part_parameters` | Parameters | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | A narrowing filter is missing, template identity is ambiguous/conflicting, the requested window exceeds 1,000 rows, or bounded scanning cannot establish complete row-ID ordering. |
| `search_category_parameter_defaults` | Category defaults | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Category/template identity is invalid, inheritance is requested without a category, or a filtered scan exceeds 1,000 links. |
| `audit_parameter_consistency` | Parameter audit | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Filters are invalid or a complete audit exceeds the shared 1,000 upstream request-and-record budget. |
| `search_companies` | Company lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Supplier/manufacturer identity is ambiguous. |
| `search_suppliers` | Company lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Supplier role is unclear. |
| `search_manufacturers` | Company lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Manufacturer role is unclear. |
| `get_company` | Company administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable company identity is invalid. |
| `search_supplier_parts` | Sourcing links | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Filters or pagination are invalid. |
| `get_supplier_part` | Sourcing links | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable supplier-part identity is invalid. |
| `search_manufacturer_parts` | Sourcing links | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Filters or pagination are invalid. |
| `get_manufacturer_part` | Sourcing links | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable manufacturer-part identity is invalid. |
| `search_stock_locations` | Stock lookup | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Location name/path is ambiguous. |
| `get_stock_location` | Stock administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable location ID is invalid. |
| `search_stock_location_types` | Stock administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Existing location-type identity is ambiguous. |
| `get_stock_location_type` | Stock administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable location-type ID is invalid. |
| `search_stock_items` | Stock lookup and receipt recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Existing stock may duplicate requested initial stock, or a purchase receipt needs source-order readback. |
| `get_stock_item` | Stock administration | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable stock-item ID is invalid. |
| `list_attachments` | Attachments | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Target object is ambiguous. |
| `get_attachment_metadata` | Attachments | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Attachment ID is missing or ambiguous. |
| `download_attachment` | Attachments | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Operator may mean stored-link metadata versus an external link target, or original file versus explicit thumbnail mode. |
| `download_part_image` | Attachments | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Operator may mean a generic attachment rather than the current primary image, or original image versus explicit thumbnail mode. |
| `preview_purchase_order_with_lines` | Purchasing preview | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Supplier part, price, quantity, or currency is ambiguous. |
| `search_purchase_orders` | Purchasing recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Search filters do not identify the intended order. |
| `get_purchase_order` | Purchasing recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable purchase-order ID does not exist. |
| `search_purchase_order_lines` | Purchasing recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Order or supplier-part identity is unclear. |
| `get_purchase_order_line` | Purchasing recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable purchase-order-line ID does not exist. |
| `search_purchase_order_extra_lines` | Purchasing extra-line recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Purchase-order filter or bounded recovery identity is unclear. |
| `get_purchase_order_extra_line` | Purchasing extra-line recovery | Read-only | `inventree.read` | None | `readOnlyHint:true`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered` | Stable extra-line ID is invalid or unreadable. |
| `create_part` | Part entry | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `update_part` | Part entry | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `delete_part` | Part entry | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable part ID or `confirm:true` is missing, the part is still active, or stock, bill-of-materials, build, purchase-order-line, sales-order-line, variant, supplier-part, manufacturer-part, parameter, attachment, or related-part references still exist. |
| `create_part_category` | Category administration | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Same-parent identity or referenced IDs are invalid/ambiguous. |
| `update_part_category` | Category administration | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Patch identity, hierarchy safety, structural policy, or required confirmation is unresolved. |
| `set_part_parameters` | Parameters | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `delete_part_parameter` | Parameters | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable parameter-row ID or `confirm:true` is missing. |
| `create_parameter_template` | Parameters | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Required explicit fields are missing or the name collides. |
| `update_parameter_template` | Parameters | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, patch fields, or collision resolution is missing. |
| `delete_parameter_template` | Parameters | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | References remain or `confirm:true` is missing. |
| `merge_parameter_templates` | Parameters | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | A current dry-run hash, confirmation, or manual conflict decision is required. |
| `create_category_parameter_default` | Category defaults | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Existing category/template identity or duplicate resolution is missing. |
| `update_category_parameter_default` | Category defaults | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable direct-link identity, patch fields, or duplicate resolution is missing. |
| `delete_category_parameter_default` | Category defaults | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable direct-link identity or `confirm:true` is missing. |
| `bulk_propagate_part_parameters` | Parameter propagation | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Explicit template/value/part scope, overwrite intent, or matching dry-run confirmation is missing or stale. |
| `create_company` | Company entry | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `create_supplier_part` | Supplier link | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `create_manufacturer_part` | Manufacturer link | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `update_company` | Company administration | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Patch, role-removal confirmation, or dependency state is unresolved. |
| `update_supplier_part` | Sourcing links | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Reference or normalized duplicate identity is unresolved. |
| `update_manufacturer_part` | Sourcing links | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Reference or normalized duplicate identity is unresolved. |
| `upsert_part_with_supplier_and_manufacturer` | Part workflow | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | See Registered Write Tools. |
| `create_stock_item` | Initial stock | Operational | `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Existing stock at the requested location may duplicate the new item. |
| `create_initial_stock_entry` | Initial stock workflow | Operational | `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Part or location search is ambiguous, quantity/status is invalid, or existing stock at the requested location may duplicate the new item. |
| `create_stock_location` | Stock administration | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | References, normalized identity, or bounded duplicate proof is unresolved. |
| `update_stock_location` | Stock administration | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Ordinary metadata patch, references, duplicate identity, or read-back is unresolved. |
| `restructure_stock_location` | Stock hierarchy | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Safe hierarchy target or matching reviewed plan is missing or stale. |
| `update_stock_item_metadata` | Stock metadata | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Approved metadata fields or matching reviewed plan are missing or stale. |
| `adjust_stock_quantity` | Stock adjustment | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, audit reason, quantity delta, or matching confirmation plan is missing or stale. |
| `set_stock_status` | Stock status | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, audit reason, supported status, or matching confirmation plan is missing or stale. |
| `stocktake_adjustment` | Stocktake | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, audit reason, absolute observed quantity, or matching confirmation plan is missing or stale. |
| `deplete_stock_item` | Stock depletion | Destructive | `inventree.read`, `inventree.write`, `inventree.operational`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, safe delete-on-deplete state, audit reason, or matching reviewed confirmation is missing or stale. |
| `transfer_stock_item` | Stock transfer | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable source/destination identity, safe complete-quantity source state, audit reason, or matching reviewed confirmation is missing or stale. |
| `create_purchase_order` | Purchasing | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Supplier role, reference, dates, or destination is invalid. |
| `add_purchase_order_line` | Purchasing | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Supplier part does not belong to the order supplier or price data is incomplete. |
| `update_purchase_order_line` | Purchasing | Write | `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | No PATCH fields are supplied or supplier consistency fails. |
| `delete_purchase_order_line` | Purchasing | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable line ID or `confirm:true` is missing, the line already has received quantity, or stock items already reference this order and supplier part. |
| `create_purchase_order_extra_line` | Purchasing extra line | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Order, unique reference, field validation, or recovery state is unresolved. |
| `update_purchase_order_extra_line` | Purchasing extra line | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:true`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable identity, patch, unique target reference, or read-back is unresolved. |
| `delete_purchase_order_extra_line` | Purchasing extra line | Destructive | `inventree.read`, `inventree.write`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable extra-line ID or `confirm:true` is missing. |
| `create_purchase_order_with_lines` | Purchasing workflow | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Retry identity or supplier-part validation is ambiguous. |
| `issue_purchase_order` | Purchasing | Write | `inventree.read`, `inventree.write` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Order is not pending, `confirm_issue:true` is missing, or the current-state issue `plan_hash` is absent or stale. |
| `receive_purchase_order_items` | Purchasing receipt | Operational | `inventree.read`, `inventree.write`, `inventree.operational` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Order is not placed; line, quantity, or destination is invalid; or matching dry-run confirmation is missing. Read scope guarantees source-order stock recovery after an ambiguous result. |
| `upload_attachment` | Attachments | Write | `inventree.write`, `inventree.upload` | Inline bytes; STDIO allowlisted local path | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Filename/content duplicates an existing attachment without explicit duplicate intent. |
| `upload_attachment_from_url` | Attachments | Write, open-world | `inventree.write`, `inventree.upload` | HTTP(S) URL only | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:true` | `registered_with_oauth_scope_guard` | URL policy rejects the target or filename/content duplicates an existing attachment without explicit duplicate intent. |
| `create_link_attachment` | Attachments | Write | `inventree.write`, `inventree.upload` | HTTP(S) link only, no fetch | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | URL has unsupported scheme, credentials/userinfo, fragment, local path shape, or duplicates an existing link without explicit duplicate intent. |
| `update_attachment_metadata` | Attachments | Write | `inventree.write`, `inventree.upload` | None | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable attachment ID is missing or no PATCH fields are supplied. |
| `delete_attachment` | Attachments | Destructive | `inventree.write`, `inventree.upload`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable attachment ID is missing or `confirm:true` is missing. |
| `set_primary_image` | Attachments | Write | `inventree.write`, `inventree.upload` | Existing attachment/image ID | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Multiple candidate images exist or replacement lacks `confirm:true`. |
| `set_company_image` | Company images | Write | `inventree.read`, `inventree.write`, `inventree.upload` | Inline bytes; STDIO allowlisted local path | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Source, raster contract, stable identity, replacement confirmation, or digest read-back is unresolved. |
| `set_company_image_from_url` | Company images | Write, open-world | `inventree.read`, `inventree.write`, `inventree.upload` | HTTP(S) URL only | `readOnlyHint:false`, `destructiveHint:false`, `idempotentHint:false`, `openWorldHint:true` | `registered_with_oauth_scope_guard` | URL policy, raster contract, replacement confirmation, or digest read-back is unresolved. |
| `clear_company_image` | Company images | Destructive | `inventree.read`, `inventree.write`, `inventree.upload`, `inventree.destructive` | None | `readOnlyHint:false`, `destructiveHint:true`, `idempotentHint:false`, `openWorldHint:false` | `registered_with_oauth_scope_guard` | Stable company ID or `confirm:true` is missing, or exact null read-back is unresolved. |

## Future Tools

These tools are planned but outside milestone 1 unless the plan is explicitly changed:

- BOM tools: `get_bom`, `validate_bom`, `add_bom_item`, `update_bom_item`, `remove_bom_item`, `import_bom_rows`.
- Remaining purchase-order tool: `close_purchase_order`.
- Build tools: `search_build_orders`, `create_build_order`, `allocate_build_stock`, `issue_build_outputs_to_stock`, `complete_build_order`.
- Bulk imports: `import_parts`, `import_supplier_parts`, `import_stock_items`, `import_purchase_order_rows`.

## Deferred Scope

Sales/customer workflows, sales-order tools, return-order tools, transfer-order tools, customer-role defaults, build attachments, return attachments, and transfer attachments are deferred for the first release. Company primary-image tools operate on an existing stable company regardless of role and do not expand customer or sales administration. F-S27's direct relocation of one existing stock item is distinct from the deferred transfer-order workflow; partial stock splits and multi-item relocation batches remain deferred to F-S28 and F-S29.
